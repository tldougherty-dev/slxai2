/** Shared horizontal layout for public pages (matches nav + footer). */
export const PUBLIC_SECTION_PADDING = 'px-4 sm:px-6 lg:px-8';

/** Full-width content column capped at ~80% of 7xl (1024px), centered. */
export const PUBLIC_CONTENT_WIDTH = 'mx-auto w-full max-w-5xl';
